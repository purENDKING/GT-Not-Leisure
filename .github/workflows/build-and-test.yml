
name: Build and test

on:
  pull_request:
    branches: [ master, main, v2.7.2 ]
  push:
    branches: [ master, main, v2.7.2 ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
